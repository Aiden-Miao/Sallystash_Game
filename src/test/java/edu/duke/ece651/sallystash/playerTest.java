package edu.duke.ece651.sallystash;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

public class playerTest {
  /*
  @Test
   public void test_() {
    stash G1 = new stash('G');
    stash G2 = new stash('G');
    stash B1 = new stash('B');
    player p1 = new player(20,10);
    int[] temp1 = {5,5};
    int[] temp2 = {4,5};
    int[] temp3 = {-1,1};
    p1.put_stash(temp1, 'H', B1);
    p1.put_stash(temp2, 'V', G2);//overlap
    p1.put_stash(temp3, 'H', G1);//invalid
    p1.dig(-1, -1, p1.getboard());//invalid
    p1.dig(5, 5, p1.getboard());
    p1.dig(3, 3, p1.getboard());
    display disp = new display();
    //disp.display_myboard(disp.draw_opponentboard(p1.getboard()));
    //disp.display_board(disp.draw_myboard(p1.getboard()));
    disp.display_board(disp.draw_wholeboard(p1.getboard(), p1.getboard()));
  }
  */
}
